import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wisekookaburrawiggle',
  appName: 'wise-kookaburra-wiggle',
  webDir: 'dist'
};

export default config;
